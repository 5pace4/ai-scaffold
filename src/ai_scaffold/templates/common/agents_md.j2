# {{ project_name }} — Agents Context

## Project Summary

**{{ project_name }}** is a **{{ profile }} profile** AI application.

- LLM: `{{ llm_provider }}` (`{{ llm_model }}`)
- Vector DB: `{{ vector_db }}`
- Embeddings: `{{ embedding_model }}`

## Repository Layout (key files for agents)

| File | Purpose |
|---|---|
| `src/config.py` | All settings — read this first |
| `src/models.py` | API schemas |
{% if profile in ["rag", "full"] %}
| `src/services/rag_pipeline.py` | Main RAG orchestration |
| `src/components/hybrid_retriever.py` | Retrieval logic |
| `src/services/semantic_cache.py` | Cache layer |
| `src/security/input_guard.py` | Input validation |
{% endif %}
{% if profile in ["agent", "full"] %}
| `src/agents/document_grader.py` | Doc relevance grading |
| `src/agents/adaptive_router.py` | Query routing |
| `src/agents/tools/` | Tool implementations |
{% endif %}

## Running Tests

```bash
uv run pytest tests/ -v
```

## Linting & Type Checking

```bash
uv run ruff check src/ tests/
uv run mypy src/
```
