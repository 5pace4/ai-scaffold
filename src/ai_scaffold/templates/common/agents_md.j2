# {{ project_name }} — Agents Context

## Project Summary

**{{ project_name }}** — `{{ profile }}` profile AI application.

- LLM: `{{ llm_provider }}` / `{{ llm_model }}`
- Vector DB: `{{ vector_db }}`
- Embeddings: `{{ embedding_model }}`

## Key Files

| Path | Purpose |
|---|---|
| `src/configs/config.py` | All settings — read this before touching anything |
| `src/main.py` | Entry point / API bootstrap |
| `src/dependencies.py` | FastAPI dependency injection |
{% if profile in ["rag", "full"] %}
| `src/ingestion/` | Document loading, chunking, cleaning |
| `src/embedding/embedder.py` | Embedding model wrapper |
| `src/vectorstore/retriever.py` | Top-k vector search |
| `src/rag/pipeline.py` | End-to-end RAG orchestration |
| `src/evaluation/evaluator.py` | RAG quality evaluation |
{% endif %}
{% if profile in ["agent", "full"] %}
| `src/agents/base_agent.py` | Abstract agent class |
| `src/workflows/agent_loop.py` | ReAct / tool-use loop |
| `src/tools/` | Pluggable tool definitions |
| `src/memory/` | Short-term and long-term memory |
| `src/guardrails/validators.py` | Input / output safety checks |
{% endif %}
| `src/api/routes.py` | HTTP route definitions |
| `src/cache/redis_client.py` | Semantic caching |
| `src/monitoring/tracing.py` | OpenTelemetry tracing |

## Running Tests

```bash
uv run pytest tests/ -v
```

## Linting & Type Checking

```bash
uv run ruff check src/ tests/
uv run mypy src/
```
