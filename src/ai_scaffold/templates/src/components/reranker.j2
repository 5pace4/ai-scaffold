"""Cross-encoder reranker — improves retrieval precision."""

from src.models import Document


class Reranker:
    """Score (query, document) pairs and return top-k by relevance."""

    def __init__(self, model: str = "cross-encoder/ms-marco-MiniLM-L-6-v2") -> None:
        self.model = model

    async def rerank(self, query: str, documents: list[Document], top_k: int = 5) -> list[Document]:
        """Return documents sorted by cross-encoder relevance score."""
        # TODO: load cross-encoder and score each (query, doc.content) pair
        raise NotImplementedError
