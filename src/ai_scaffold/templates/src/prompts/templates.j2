"""Prompt templates — raw Jinja2 / f-string prompt definitions."""

RAG_SYSTEM = """\
You are a helpful assistant for {{ project_name }}.
Answer questions using only the provided context.
If the context does not contain enough information, say so clearly.
"""

RAG_USER = """\
Context:
{context}

Question: {query}

Answer:"""

REWRITE_SYSTEM = """\
Rewrite the user's query to be more specific and retrieval-friendly.
Return only the rewritten query, nothing else.
"""

INTENT_SYSTEM = """\
Classify the user query into one of: rag, agent, direct, fallback.
Return only the label.
"""
{% if profile in ["agent", "full"] %}

AGENT_SYSTEM = """\
You are an autonomous agent for {{ project_name }}.
Use the available tools to answer the user's request step by step.
"""
{% endif %}
